import React from 'react';

type Props = {
    title: string,
    body: string;
    accepted: Function;
    showDecline: boolean;
}

const Modal = ({title, body, accepted, showDecline}:Props) => {
    return (
        <div>

        </div>
    );
};

export default Modal;